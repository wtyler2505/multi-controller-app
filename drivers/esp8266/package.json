{
  "name": "@multi-controller/driver-esp8266",
  "version": "1.0.0",
  "description": "ESP8266 device driver for Multi-Controller App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:hardware": "jest --testMatch='**/*.hardware.test.ts'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist coverage"
  },
  "dependencies": {
    "@multi-controller/core": "workspace:*",
    "@multi-controller/transport-network": "workspace:*"
  },
  "devDependencies": {
    "@multi-controller/tsconfig": "workspace:*",
    "@multi-controller/eslint-config": "workspace:*"
  },
  "files": [
    "dist",
    "src",
    "manifest.json"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}