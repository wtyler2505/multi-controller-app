{
  "name": "@multi-controller/telemetry",
  "version": "1.0.0",
  "description": "Real-time telemetry data collection and buffering for Multi-Controller App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:loopback": "jest --testMatch='**/*.loopback.test.ts'",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist coverage"
  },
  "dependencies": {
    "@multi-controller/core": "workspace:*",
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "@multi-controller/tsconfig": "workspace:*",
    "@multi-controller/eslint-config": "workspace:*",
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5"
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}